apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: medical-chatbot
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
